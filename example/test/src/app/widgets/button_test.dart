import 'package:example/src/app/widgets/button.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('button ...', (tester) async {
    var button = const HomePageButton(text: 'test');
    // expect(find.byType(HomePageButton), findsOneWidget);
  });
}
